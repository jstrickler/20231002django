"""
URL Configuration for nc
"""
from django.urls import path
from . import views   # import views from app

app_name = "nc"

urlpatterns = [
    # add url patterns for the nc app here

    # Examples:
    # path('', views.home, name='home'),
    # path('thing', views.thing, name='thing'),
]
